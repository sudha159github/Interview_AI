# Normalise line endings in the repository; check out with the platform's native endings
* text=auto

# Shell scripts must always use LF (they run in Linux containers and CI)
*.sh text eol=lf

# Better diffs for C#
*.cs diff=csharp
